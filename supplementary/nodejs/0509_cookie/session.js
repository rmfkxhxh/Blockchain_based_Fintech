/* 이것은 세션
const seesion = {
    web7722:true,
    ingoo:true,
} 
*/
const { map } = require("cheerio/lib/api/traversing");

 

// 구분값을 쿠키로 활용
// 세션 쿠키

/*
    쿠키 : 쿠키
    세션 : 쿠키 + 세션
*/

/*
    데이터를 계속 가지고 다녀야한다.
    ?idx=1&userid=web7722
*/

map((value,key)=> {})
requestIdleCallback((acc,value)=> {}, 0)